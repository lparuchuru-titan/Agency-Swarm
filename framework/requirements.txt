# Unified Dev Swarm — LangGraph orchestration (pip install required for swarm runs)
langchain>=0.2.16,<0.3
langchain-anthropic>=0.1.23,<0.2
langchain-core>=0.2.38,<0.3
langgraph>=0.2.28,<0.3
httpx>=0.27.0
rich>=13.7.0
schedule>=1.2.2

# FleetView dashboard (serve_fleet.py) uses stdlib only — no pip install required
