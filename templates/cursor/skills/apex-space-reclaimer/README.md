# Apex Space Reclaimer

Read-only Apex character-limit reclaim analyzer for any Salesforce org.

```bash
node scripts/analyze.mjs -o YOUR_ORG_ALIAS --current-pct 92 --target-pct 75
```

See `SKILL.md` and `reference.md`.
