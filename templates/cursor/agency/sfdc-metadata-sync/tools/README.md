# Tools for `sfdc-metadata-sync`

Agency Swarm pattern: agent-specific scripts live here.
This project maps tools to the global skill:

- `~/.cursor/skills/sfdc-metadata-sync/scripts/`
- `.cursor/skills/sfdc-metadata-sync/scripts/` (if present)

- `.cursor/skills/sfdc-metadata-sync/scripts/build-delta-package.py`
- `~/.cursor/skills/sfdc-metadata-sync/scripts/build-delta-package.py`

Add new tools as Python scripts in this folder or extend the skill scripts.
